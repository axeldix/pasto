import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({

  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'stretch',
    margin: 25
  },

  screenTitle: {
    fontSize: 22,
  },

  textInput: {
    paddingTop: 10,
    borderBottomWidth: 1,
    color: 'black',
    fontSize: 17,
  },
});
